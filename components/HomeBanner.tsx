import React from "react";
import HeroSlider from "./HeroSlider";

const HomeBanner = () => {
  return (
    <div className="py-4 md:py-8 rounded-xl px-0 md:px-0">
      <HeroSlider />
    </div>
  );
};

export default HomeBanner;